# =================================
# MindshiftR API — Environment Variables
# =================================

# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/mindshiftr?schema=public"

# Redis (for refresh tokens & caching)
REDIS_URL="redis://localhost:6379"

# JWT Secrets (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_ACCESS_TOKEN_SECRET="your-access-token-secret-change-in-production"
ACCESS_TOKEN_EXPIRES_IN="15m"

# OpenAI (for AI story generation & ZenBot)
OPENAI_API_KEY="sk-..."

# Frontend URL (for CORS & invitation links)
FRONTEND_URL="http://localhost:5173"

# Server
PORT=4000
NODE_ENV="development"

# Email Service (future: SendGrid)
# SENDGRID_API_KEY="SG...."
# FROM_EMAIL="noreply@mindshiftr.com"

# Sentry (future: error tracking)
# SENTRY_DSN="https://..."
